<br>
<%= link_to 'Category Index', categories_path %> 
<br>

<center><h1><%= @category.title %></h1></center>
<br> 

<h2>Quizzes</h2>
<hr>
<ul>
  <% @category.quizzes.each do |q| %>
    <li>
      <%= link_to q.title, category_quiz_path(@category, q) %><br>
      <%= q.body %><br>
      <%= q.level %>
    </li>
    <hr>
  <% end %>  
</ul>
